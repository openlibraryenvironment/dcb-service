### Development config

hosts:
  sandbox:
    type: org.olf.reshare.dcb.core.interaction.sierra.SierraLmsClient
    client:
      base-url: "https://sandbox.iii.com"
      key: ${SANDBOX_API_KEY:""}
      secret: ${SANDBOX_API_SECRET:""}
      ingest: false
    
agencies:
  testAgency1:
    hostLms: sandbox

# micronaut:
#   security:
#     authentication: bearer
#     token:
#       jwt:
#         generator:
#           access-token:
#             expiration: 600
#         signatures:
#           secret:
#             generator:
#               secret: "${token.secret:secretsmustbeatleast256bitssothats32chars}"
#               base64: true
#               jws-algorithm: HS256
#           jwks:
#             keycloak:
#               url: ${KEYCLOAK_CERT_URL:""}
